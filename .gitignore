# Claude Code (per-user AI config, not project docs)
.claude/

# MCP config (contains local paths)
.mcp.json

# OS files
.DS_Store
Thumbs.db
desktop.ini

# Editor files
*.swp
*.swo
*~
.vscode/
.idea/

# Test artifacts & Playwright screenshots
test*.pdf
test*.png
*.screenshot.png
responsive-*.png
screenshots/
playwright-report/
test-results/
tests/golden/actual/

# Secrets (Vercel/Sentry tokens, etc.) — never commit
.env
.env.*
!.env.example

# Temp files
*.tmp
*.log
node_modules/

# Agent skills working dir (installed via skills CLI; lockfile is tracked instead)
.agents/

# Wild PDF corpus — REAL user documents, local only, NEVER committed.
# Founder ruling 2026-07-28: "the disk is full of wild PDF. you can use those.
# as long as the PDF file don't go to the public opensource repo".
# The harness is committed; the documents are not.
tests/fixtures/wild/
